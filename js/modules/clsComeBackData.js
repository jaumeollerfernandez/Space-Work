class clsComeBackData{
    constructor(){
        //Pasar un registro de los divs creados para guardarlos en un JSON con cada elemento

        // Registrar el JSON creado en el LocalStorage.

        //Actualizar cada vez que se destruye o se crea un elemento nuevo. Hará todo el
        //ciclo necesario cada vez

        //Al cargar la página, registrará mediante el Object.setPrototypeOf los elementos con
        //sus propiedades insertadas de nuevo. Sería interesante hacer una actualización de la
        //posición en la que se encuentra?
    }
}